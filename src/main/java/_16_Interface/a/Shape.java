package _16_Interface.a;

// Square, Triangle 클래스를 만들고, Shape을 이삭받아주세요
// 두 클래스 모두 width, height 필드 존재

// Manager -> 1 넓이당 100으로 계산하는 메서드(calcPrice)
public interface Shape {
    double getArea(); // 넓이를 리턴
}
